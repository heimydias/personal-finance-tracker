personal-finance-tracker:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    hikari:
      maximum-pool-size: 1
  h2:
    console:
      enabled: false
      path: /h2-console
      settings:
        web-allow-others: false
  jpa:
    open-in-view: false
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
    clean-disabled: false
    validate-on-migrate: true

  jwt:
    users:
      default-password: ${DEFAULT_PASSWORD}
    admin:
      email: ${ADMIN_EMAIL}
      password: ${ADMIN_PASSWORD}
    access-token:
      secret: ${JWT_ACCESS_TOKEN_SECRET}
      expiration-minutes: ${JWT_ACCESS_TOKEN_EXPIRATION_MINUTES}
    refresh-token:
      secret: ${JWT_REFRESH_TOKEN_SECRET}
      expiration-days: ${JWT_REFRESH_TOKEN_EXPIRATION_DAYS}

  springdoc:
    swagger-ui:
      enabled: true
  openapi-servers-urls: http://localhost:${server.port}${server.servlet.context-path}, http://127.0.0.1:${server.port}${server.servlet.context-path}